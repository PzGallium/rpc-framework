# Local dev: start Zookeeper (port 2181)
# Run: docker-compose up -d
# Stop: docker-compose down
version: '3'
services:
  zookeeper:
    image: zookeeper:3.8
    container_name: rpc-framework-zk
    ports:
      - "2181:2181"
    restart: unless-stopped
