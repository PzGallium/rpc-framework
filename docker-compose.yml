# 本地开发用：一键启动 Zookeeper，默认端口 2181
# 使用：docker-compose up -d
# 停止：docker-compose down
version: '3'
services:
  zookeeper:
    image: zookeeper:3.8
    container_name: rpc-framework-zk
    ports:
      - "2181:2181"
    restart: unless-stopped
